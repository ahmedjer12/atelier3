resolver 127.0.0.11 valid=10s;

server {
    listen 80;

    location / {
        set $backend "app-green:5000";
        proxy_pass http://$backend;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
